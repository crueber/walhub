# build output
/walhub
/walhub-server
/bin/
*.test
*.out
.cover/

# web
web/dist/*
!web/dist/.keep
web/node_modules/

# runtime data
/data/
*.json.tmp
walgit-state.json
covergate/

# Forgejo #253: global gitignores blanket-ignore `tags` (ctags output) —
# re-include the package dir so internal/tags/* is tracked.
!internal/tags/
